# Copy to .env for local dev. Set the same keys in Vercel → Project → Settings → Environment Variables.

PREVIEW_ADMIN_USER=BannerWave

# Generate with: npm run hash-password -- "your-secret-password"
PREVIEW_ADMIN_PASSWORD_HASH=

# Long random string (32+ characters)
PREVIEW_SESSION_SECRET=
